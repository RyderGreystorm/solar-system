name: Testing runner
on:
    workflow_dispatch:
        inputs:
            apply:
                description: for applying
                required: true
jobs:
    testing-me-runners:
        runs-on: [self-hosted,Linux,X64,prod,gpu,ai]
        steps:
            - name: Test with echo
              run: |
                echo "Testing the gpu enabled runner"
                sleep 10s